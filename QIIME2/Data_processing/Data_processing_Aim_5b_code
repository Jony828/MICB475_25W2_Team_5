#GC
qiime feature-table filter-features \
 --i-table gc_table_age40-80.qza \
 --p-min-frequency 5 \
 --o-filtered-table gc_feature-frequency-filtered-table.qza

mkdir picrust

qiime tools export \
  --input-path gc_feature-frequency-filtered-table.qza \
  --output-path picrust

qiime tools export \
  --input-path gc_rep-seqs.qza \
  --output-path picrust

picrust2_pipeline.py \
  -s dna-sequences.fasta \
  -i feature-table.biom \
  -o picrust_out
